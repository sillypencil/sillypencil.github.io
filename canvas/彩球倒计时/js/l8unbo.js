function move(){
//动画函数
}

document.getElementById('zhengxu').onclick = function(){
	var = timer = setInterval(function(){
		var curNum;//计算
		var next = curNum +1;
		move(curNum, next)
	},2000)
	
}
document.getElementById('daoxuxu').onclick = function(){
	var = timer = setInterval(function(){
		var curNum;//计算
		var next = curNum +1;
		move(curNum, next)
	},2000)
}


li[i].onclick = function(){
	var curNum;//计算
	move(curNum, i)
}
